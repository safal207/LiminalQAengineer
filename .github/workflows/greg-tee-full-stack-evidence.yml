name: Greg Tee Full Evidence Stack (superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Use v2 workflow
        run: |
          echo "This workflow is preserved for history only."
          echo "Use .github/workflows/greg-tee-full-stack-evidence-v2.yml."
