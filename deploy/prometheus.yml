global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'liminalqa-ingest'
    static_configs:
      - targets: ['liminalqa-ingest:8080']
    metrics_path: '/metrics'
